import React from 'react';
import VideoViewer from './video-viewer';

const SmartVideoViewer: React.FC = props => {
  return <VideoViewer />;
};

export default SmartVideoViewer;

function useSlides(videoId: string) {}
